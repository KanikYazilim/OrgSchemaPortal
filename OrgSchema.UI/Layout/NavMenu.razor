<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand d-flex align-items-center" href="">
            <i class="bi bi-diagram-3-fill me-2 fs-4 text-warning"></i>
            <span class="fw-bold">Kurumsal Portal</span>
        </a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="nav flex-column mt-4">
        <div class="nav-item px-3 mb-2">
            <NavLink class="nav-link custom-nav-link" href="" Match="NavLinkMatch.All">
                <i class="bi bi-house-door-fill me-3 fs-5"></i> Ana Sayfa
            </NavLink>
        </div>
        
        <div class="nav-item px-3 mb-2">
            <NavLink class="nav-link custom-nav-link" href="organization-list">
                <i class="bi bi-people-fill me-3 fs-5"></i> Personel Listesi
            </NavLink>
        </div>
        
        <div class="nav-item px-3 mt-auto mb-2">
            <NavLink class="nav-link custom-nav-link" href="admin">
                <i class="bi bi-gear-fill me-3 fs-5"></i> Yönetim Paneli
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
